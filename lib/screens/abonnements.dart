import 'package:flutter/material.dart';

class abonnements extends StatelessWidget {
  const abonnements({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: null,
      body: Center(child: Text("abonnements"),)
    );
  }
}